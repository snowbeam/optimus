apiVersion: v1
kind: Service
metadata:
  name: {{ include "snowbeam-optimus.fullname" . }}
  labels:
    {{- include "snowbeam-optimus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snowbeam-optimus.selectorLabels" . | nindent 4 }}
